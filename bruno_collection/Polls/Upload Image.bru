meta {
  name: Upload Image
  type: http
  seq: 13
}

post {
  url: http://localhost:3000/uploads
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  file: @file(/path/to/your/image.jpg)
}

settings {
  encodeUrl: true
  timeout: 0
}
